

var password = "8reindeer"
var guess = prompt("Guess the password: ")

if (guess === password) {
      alert("Well done!");
  }
else {
      alert("Sorry, the password is " + password + " . ");
  }

